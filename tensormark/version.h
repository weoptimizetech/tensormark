#pragma once
// Keep in sync with project.version in pyproject.toml (test_packaging.py checks).
// Included by both setup.py's extension and build.sh's development build.
#define TENSORMARK_VERSION "0.4.1"
